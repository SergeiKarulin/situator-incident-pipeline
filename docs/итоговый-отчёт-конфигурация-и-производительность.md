# Итоговая конфигурация, производительность и рекомендация по сервису

**Объединение сработок в инциденты для Ситуатора 2.0 · для руководителя разработки**

## 1. Итоговая конфигурация

Конвейер на проверенных открытых компонентах, прогнан на **двух боевых выгрузках** (1–4 июня и
17–19 июня 2026), 9+ камер, 6 объектов. Вход — **только видео** (как в проде):

```
сработка (clip.mp4) → кадры из клипа (NVDEC в проде / ffmpeg) → детектор деградации сигнала
  → YOLO11x@1280 на GPU (люди/техника/животные, пер-классовые пороги, стабилизация счёта)
  → подавление статичных объектов сцены → темпоральные сессии (правило ядра)
  → межкамерная склейка по графу соседних камер → карточки + описания (словарь, без LLM)
  ↘ на ФРОНТАЛЬНЫХ камерах (ворота): плате-детектор → РФ-OCR (наша) → confidence+кратность-гейт → госномер
выход: карточка инцидента {тип, период, камеры, описание, госномер?, trigger_ids} + события ре-флага
```

- **Детекция:** YOLO11x@1280 (не дешёвая yolo11s) — ловит мелкое/ночное, что лёгкая модель теряет
  (ночной человек на тёмной камере: yolo11s не увидел, yolo11x нашёл person 0.34).
- **Номера:** собственная РФ-OCR (дообученный fast-plate-ocr, CCT-S v2, кириллица), held-out
  char-acc 99.99%; глобальная OCR путала У→М/Н→В — наша чинит. Только на фронтальных камерах.
- **Безопасность:** пол «0 потерь REAL» — поштучная проверка на каждом прогоне (не «by construction»).

## 2. Результаты — два эксперимента на боевых выгрузках

Главная метрика — **потребность в реакциях оператора** (карточки + ре-флаги) и **потерянные инциденты**.

### Эксперимент 1 — данные 1–4 июня 2026 (96 ч), Ситуатор исходный

| Метрика | Ситуатор (исходный) | Наш конвейер |
|---|---|---|
| Реакций оператора всего | 3 762 | **1 359** (801 карточка + 558 ре-флагов) |
| Реакций в час | ≈39/ч | **≈14/ч** |
| Сжатие | 1.42× | **8.6×** |
| Межкамерных | 0 | 61 |
| Потеряно REAL | — (режим статистики, разбора не было) | **0 из 178** |

### Эксперимент 2 — данные 17–19 июня 2026 (≈56 ч), Ситуатор УЖЕ улучшен

Между экспериментами программисты докрутили группировку Ситуатора (межкамерное **0→8**, сжатие
**1.69→4.86×**) — улучшение неоспоримо. **Но этого не хватило:**

| Метрика | Ситуатор (улучшенный) | Наш конвейер |
|---|---|---|
| Реакций оператора всего | 1 074 | **787** (471 карточка + 316 ре-флагов) |
| Реакций в час | ≈19/ч | **≈14/ч** |
| Сжатие | 4.86× | **10.98×** |
| Межкамерных | 8 | 26 (на спорной паре, см. §6) |
| Потеряно REAL | 0 из 37 (разбирали люди) | **1 из 37** (стоп-кадровый промах) |

### Главное — нагрузка и потери

| | Эксп.1 Ситуатор | Эксп.1 наш | Эксп.2 Ситуатор↑ | Эксп.2 наш |
|---|---|---|---|---|
| **Реакций/час** | 39 | **14** | 19 | **14** |
| **Потеряно инцидентов** | — | **0/178** | 0/37 | 1/37 |

Улучшенный Ситуатор срезал свою нагрузку вдвое (39→19/ч), но наша всё равно ниже (14/ч). Цена —
1 потерянный REAL (стоп-кадровый промах, восстановим видео-гейтингом, §6б).

**Качество группировки (зрением):** наше сжатие корректное, не пересклейка — крупная карточка =
34 мин непрерывной активности во дворе (один инцидент; под-события всплывают ре-флагами).
Межкамерное «26 vs 8» — не победа: обе стороны клеят на сомнительной паре (граф соседства угадан),
реальный коридор (гаражи) в выгрузке простаивал.

### Наше отличие — читаемое описание + ре-флаги

У Ситуатора в карточке только id, время, сенсоры. Мы на каждую карточку даём **описание
(≤160 симв., словарём, без LLM)** и **события ре-флага** при эскалации. Примеры наших описаний:

> Движение: человек, собака, легковая машина — камера двора + камера ворот; утром 04:01–04:48 UTC;
> 266 сработок; переход между камерами
> Помехи сигнала — камера площадки; ночью 00:03–00:59 UTC; 136 сработок; детектор движения штормит от помех
> Средовой шум — камера площадки; вечером 17:15–18:18 UTC; 136 сработок

Оператор читает суть, а не сопоставляет id/время; эскалацию видит ре-флагом (журнал — приложение).

### Эксперимент 3 — две доработки, прогнаны на обоих блоках

Поверх базового конвейера испытаны два улучшения; обе проверены **кросс-блочно** (калибровка на
одном блоке, замер на другом — без подгонки под выборку).

**Видео-гейтинг (motion-страховка)** — на обоих блоках **нейтрален** (recall-safe, метрики не
меняет). Единственная потеря REAL (Эксп.2, `6369614`) — неподвижный перекрытый объект без
motion-сигнала, страховкой не возвращается; на Эксп.1 терять нечего (0 потерь). Сокращение кадров
под motion регрессировало recall (8 потерь вместо 1) — откатили к 4 проверенным кадрам. Фича
вынесена в конфиг, **по умолчанию OFF**; включать после валидации на бо́льшем объёме (ADR-021).

**Пер-камерная калибровка (день/ночь)** — умеренная срезка реакций **без потери recall**:

| Блок | Реакций (глобальные пороги) | Реакций (пер-камерные) | Потеря REAL |
|---|---|---|---|
| Эксп.1 (1–4 июня) | 1 359 | **1 336** (−1.7%) | 0 → 0 |
| Эксп.2 (17–19 июня) | 787 | **765** (−2.8%) | 1 → 1 |

Выигрыш скромный — ожидаемо на холодном старте (REAL редкий, мало примеров на камеру). Полная отдача
пер-камерного классификатора требует накопления данных по камере + сезонности (см.
`docs/руководство-по-внедрению.md`). Главное — **recall не пострадал** (кросс-блочная проверка):
подход безопасен и растёт с объёмом данных.

## 3. Производительность и ёмкость (вопрос про 200+ камер)

Замер — нативно на Apple M5 (MPS), полный прогон 8 692 сработок:

| Показатель | Значение (8 692 сработки, 25 мин) |
|---|---|
| Пик RAM | **1 874 МБ** |
| Пик VRAM (GPU) | **1 184 МБ** (5% от 24 ГБ) |
| CPU (эквивалент ядер, из 18) | **0.2 средн. / 2.0 пик** |
| YOLO11x@1280 на активную сработку | **261 мс** (p95 314) |
| Пропускная способность узла (последовательно) | **3.8 активных сраб/с** |

Нагрузка определяется темпом сработок (сработка — событие детектора движения, не непрерывный поток).
Наш контур 9 камер даёт пик 0.75 сработки/с (≈0.08/с на камеру). Один GPU **аналога 3090** (≈×1.6 к
M5-MPS) при YOLO11x@1280 тянет ≈ **6–7 активных сработок/с**.

**Сколько камер, как наши, обслуживает один такой GPU в реальном времени:**

| Режим | Камер на один GPU |
|---|---|
| Последовательно (как замерено) | **≈ 75** |
| С батчингом GPU (×3–10, см. §3а) | **≈ 200–700** |

Расход на один GPU: VRAM 1.2 ГБ из 24 (помещается ещё ALPR ~3 ГБ и VLM-7B ~8 ГБ), CPU 0.2 ядра из 18.
Декод в проде — NVDEC (сработки — короткие event-driven клипы, нагрузка кратно ниже непрерывного потока).

**Вывод:** один GPU (аналог 3090) обслуживает ≈ 75 наших камер последовательно; **для 200+ камер на
ОДНОМ GPU нужен батчинг** (даёт 200–700) — иначе ≈ 3 GPU. Точное число — по реальному темпу сработок контура.

## 3а. Что сделать для указанной ёмкости по камерам

**Чтобы выйти на 200–700 камер на одном GPU (аналог 3090) — нужен батчинг** инференса (слать в YOLO
десятки кадров за вызов вместо 4). Это меняет цикл инференса, но **не результаты** (детекция
детерминирована покадрово, метрики не плывут) и не требует другого железа — VRAM/CPU сейчас загружены
на 5%/1%. Это главный и обязательный шаг.

**Чтобы разнести на несколько GPU/узлов** (если камер больше, чем тянет один): шардировать
**по ОБЪЕКТУ** (соседство камер всегда внутри объекта → межкамерная склейка остаётся локальной воркеру),
с сохранением порядка сработок по времени внутри шарда. Логику переписывать не нужно — кроме одного
компонента: **статик-карту заменить с глобального батч-прохода на скользящую причинную** (иначе разные
воркеры дадут разное подавление и метрики разъедутся). При соблюдении этих условий результаты совпадают
с однопоточными, пол «0 потерь REAL» держится. Партиционированный приём потока — часть стримингового API (§7).

## 4. Принятые технические решения

- **YOLO11x@1280 на GPU вместо лёгкой модели на CPU.** Замер: с дешёвой моделью CPU успевал, но
  ночь/мелочь терялись; точная модель требует GPU (на CPU 0.49 сраб/с — ниже пика). GPU оплачивает качество.
- **Стабилизация счёта объектов** — не Кальман: пер-сработочный пик + гистерезис (полосы 2/4/8/16),
  ре-флаг по входу в новую полосу, иначе спам. (Истинный трекинг с ID — на потом, под Re-ID.)
- **Ре-флаг — событие, не тихое дорастание карточки.** При эскалации (появился человек / +объекты /
  подключилась камера) карточка возвращается оператору как UPDATE. Решает риск «отработал стартовые —
  такими и остались».
- **Номера: собственная РФ-модель + гейтинг от галлюцинаций.** Confidence (min char_prob ≥0.80) +
  кратность (≥2 чтений) обвалили 55 фантомных строк до 5 доверяемых точных номеров на воротах.
- **Пол безопасности — эмпирический per-run аудит**, не доказательство по построению (REAL с
  пропущенной детекцией структурно не защищён ни в батче, ни в стриме — держит только проверка).

## 5. Учтённые особенности и неудачные попытки

- **macOS + Docker + GPU несовместимы.** Docker на Mac = Linux-VM, Apple-GPU (Metal) в неё не
  пробрасывается, CUDA на Mac нет. Поэтому для GPU-замеров пайплайн запускается **нативно** (venv,
  device=mps); рабочий конвейер остаётся в Docker (CPU) для воспроизводимости. На целевом железе
  (Linux+NVIDIA) Docker+`--gpus` работает штатно — там противоречия нет.
- **Что пробовали неудачно:** *nomeroff-net* (РФ-ALPR) — хрупкий форк (deps из git, слом torch.load) →
  бросили, дообучили **чистый** fast-plate-ocr; *ALPR на парковочной камере* — номера физически нечитаемы
  (разрешение) → только фронтальные камеры; *CoreML EP* падал на пустых детекциях → форс CPU;
  *пересборка docker ради правки .py* — стёрла кэш, тянула torch+CUDA ~40 мин → перешли на bind-mount.

## 6. Особенности формата данных (что появилось)

- **Карточка инцидента:** id, тип (`реал`/`несхлопнуто`/`среда`/`деградация`), период, камеры,
  описание (≤160 симв., словарь, без LLM), `trigger_ids`. `среда`/`деградация` — агрегаты камера-час.
- **Ре-флаг (новое):** события `CREATE/UPDATE/FINALIZE`; UPDATE = эскалация (причина+дельта) — для оператора это новая сработка по карточке.
- **Госномер (новое):** строка + уверенность; только фронтальные камеры, после гейтинга; для идентичности/белого списка/Re-ID транспорта.
- **Граф соседних камер — НУЖЕН и определяет качество межкамерных ре-флагов.** Включает
  межкамерную склейку (иначе 0 межкамерных), сравнение только внутри связанных пар. Проверка
  зрением на 10 случаях: на **подтверждённых** соседних парах (смежные боксы — человек реально
  ходит между ними) ре-флаг «подключилась камера» осмыслен; на **угаданных** парах (двор↔ворота —
  не коридор передачи субъекта) — в основном ложный. **Важно при интеграции:** в Ситуаторе уже есть
  правильная расстановка соседства камер — **подать её в конвейер вместо нашего догадочного графа**,
  тогда ложных межкамерных ре-флагов станет меньше. Граф — конфиг (`cameras.yaml`), без пересборки.
- **Камера — из `list.txt`** (`/recordings/camXX/`), не `sensor_name` (заполнен ~36%); **часовой пояс на объект** — для «утром/ночью».

## 6а. Покамерные настройки

**Что уже работает пер-камерно:**
- **Карта «мебели сцены» — автоматически по каждой камере.** Камера сама выучивает, что в её кадре
  стоит постоянно (припаркованная машина, лодка под чехлом, ряд авто): объект, висящий в ≥35%
  сработок камеры дольше 4 ч, считается фоном и не порождает карточек. Это адаптация под
  расположение конкретной камеры, без ручной настройки.
- **Роли камеры по конфигу:** какие камеры дают номера (только фронтальные), граф соседних камер
  (межкамерная склейка только внутри пар), объект и часовой пояс. Пер-объектный `τ_gap` поддержан.

Пороги детектора и детектора деградации сейчас **глобальные** (одни на все камеры) — на «лёгких»
сценах этого достаточно, на сложных (туман, ночь, контровый свет) остаётся часть шума.

**Предложение — добавить два пер-камерных механизма** (главный рычаг качества на разнородном контуре):
1. **Классификатор «реал/шум» на каждую камеру** — маленькая модель учит «как выглядит норма именно
   этой камеры», точнее общего порога; режет остаточный шум, который глобальный порог пропускает.
2. **Профиль день/ночь** — раздельные пороги/норма по времени суток (ночью и в туман сцена другая).

**Что нужно собрать с каждой новой камеры:**
- размеченные сработки **этой** камеры (реал/шум), обязательно покрывающие **и день, и ночь**;
- объём — ориентир **~1–2 недели** записи камеры: даёт избыток FALSE и десятки REAL (REAL редок ~2%,
  копится со временем); порядок — **сотни–тысячи** кадров на камеру;
- для сезонной устойчивости — данные с **разных сезонов** (снег/листва/угол солнца меняют «норму»)
  либо переобучение **раз в квартал**; профиль день/ночь снимается с нескольких суток.
- предусловие: выяснить происхождение меток реал/шум в выгрузке (кто проставлял) — без этого
  обучение рискует выучить чужой фильтр, а не правду.

**Ожидаемое влияние:** на «сложных» камерах (туман/ночь/блики) это основной источник лишних видимых
карточек — пер-камерный классификатор даёт **кратное снижение остаточного шума** на них и защищает
recall ночью; на «лёгких» камерах изменение незначительно (глобальный порог там и так работает).

## 6б. Очевидные улучшения нашего конвейера

По итогам Эксперимента 2 — что доработать, чтобы преимущество стало чистым (меньше реакций + ноль
потерь). Порядок — по ожидаемому приросту качества:

1. **Пер-камерные настройки** (§6а): классификатор «реал/шум» + профиль день/ночь на каждую камеру.
   Срезает остаточный шум на сложных сценах (туман/ночь/блики), где глобальный порог пропускает —
   это и меньше реакций, и меньше риск спутать реал с шумом. Главный рычаг качества на разнородном
   контуре.
2. **Видео-гейтинг сомнительных кандидатов** — анализ движения по кадрам клипа вместо 4 стоп-кадров.
   Закрывает стоп-кадровые потери REAL (1 потеря в Эксп.2 — событие было в видео, но не в выбранных
   кадрах). Прямой ход к «ноль потерь».
3. **Реальный граф соседства от Ситуатора.** Сейчас межкамерная склейка на угаданной паре
   (двор↔ворота) — спорна у обеих сторон. Подать настоящую расстановку соседства Ситуатора →
   межкамерные станут точными, ложные «подключилась камера» уйдут. Дёшево, только конфиг.
4. **Перекалибровка порогов под новый контур** — наши настроены на данных 1–4 июня; на 17–19
   появились две новые камеры и сместился профиль шума.

## 7. Рекомендация: сделать сервис на базе нашего репозитория

Прототип — не презентация, а **работающий код в репозитории**, который запускается тремя командами
и воспроизводит эти числа. Рекомендуем оформить его в **сервис рядом с Ситуатором 2.0**: на вход —
поток сработок (как из Интеллекта: id + клип), на выход — карточки инцидентов + события ре-флага +
номера через API/очередь.

Первоочередное (необходимое условие боя): **стриминговый API** — приём сработок, открытые сессии в
памяти, публикация карточки/ре-флага наружу; батч-логика уже эквивалентна реплею потока. Дальше по
ценности: классификатор «реал/шум» под камеру, VLM-арбитр на кандидатах (события-состояния: ворота
открыты — YOLO слепа, VLM в 24 ГБ видит), граф соседних камер по контуру, UI оператора.

Репозиторий: **https://github.com/SergeiKarulin/situator-incident-pipeline** (обезличен; данные и
имена объектов заказчика не публикуются). Этот отчёт — там же.

---

## Приложение. Реальные ре-флаги — журналы прогона

Ре-флаг — событие `UPDATE` на уже созданной карточке: при усилении обстановки (появился человек,
выросло число объектов, подключилась соседняя камера) карточка возвращается оператору наверх
очереди с пометкой причины. Ниже — три реальных случая из прогона (имена объектов убраны,
оставлены имена камер, время и краткая суть).

**Случай 1 — машина заехала → собралась группа и вошла** (камера двора + камера ворот):

| Время | Событие | Суть |
|---|---|---|
| 04:01 | CREATE | камера двора: машина |
| 04:02 | UPDATE · рост числа машин (≥2) | подъехала вторая |
| 04:02 | UPDATE · **появился человек** | ← ключевая тревога |
| 04:03 | UPDATE · **подключилась камера ворот** | межкамерный охват |
| 04:13 | UPDATE · новый объект: собака, затем грузовик | состав изменился |
| 04:18 | UPDATE · рост числа людей (≥5) | группа |
| 04:48 | FINALIZE | люди + собака + машины + грузовик, 2 камеры, 263 сработки |

**Случай 2 — из припаркованной машины вышел человек** (камера двора + камера ворот):

```
17:33  CREATE                              камера двора: машина
17:34  UPDATE · появился человек           ← из машины вышел человек (тревога)
17:34  UPDATE · рост числа людей (≥4)
17:35  UPDATE · подключилась камера ворот   межкамерный охват
17:38  FINALIZE                            машина + люди, 2 камеры
```

**Случай 3 — люди собираются, затем подъезжает транспорт** (камера двора + камера ворот):

```
10:15  CREATE                              камера двора: человек
10:16  UPDATE · рост числа людей (≥4)       людей становится больше
10:20  UPDATE · новый объект: машина        подъехал транспорт
10:36  UPDATE · подключилась камера ворот   межкамерный охват
10:36  FINALIZE                            люди + машина, 2 камеры, 106 сработок
```

Во всех трёх без ре-флагов оператор увидел бы лишь стартовое состояние («машина» / «человек») и
закрыл бы как рутину; усиление было бы похоронено внутри той же карточки. **Типы причин ре-флага:**
*появился человек* (высшая ценность), *подключилась соседняя камера* (межкамерный охват),
*новый объект*, *рост числа* (полосы 2/4/8/16). За 4 суток — 558 ре-флагов, ~⅔ содержательные;
шумные «+1 на тик» отсекаются гистерезисом и полосами.
